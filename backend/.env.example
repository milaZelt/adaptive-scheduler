# Shared-secret header required on every POST /solve request (decisions
# record: defense-in-depth on top of this service only ever being reachable
# from Next.js's own server, never a browser). Generate any long random
# string - both this service and the Next.js Route Handler that calls it
# need the same value.
SOLVER_SHARED_SECRET=
